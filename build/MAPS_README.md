# Инструкция по добавлению карт порта

## Как добавить новую карту:

### Способ 1: Предустановленные карты

1. Поместите файл изображения (PNG, JPG) в папку `public/`
2. Назовите файл, например: `port2.png`, `port3.png` и т.д.
3. Откройте файл `src/components/Content/SidebarRight/SidebarRight.jsx`
4. Найдите массив `predefinedMaps` и добавьте новую карту:

```javascript
const predefinedMaps = [
    { id: 'port1', name: 'Карта порта 1', url: '/deployport/port.png' },
    { id: 'port2', name: 'Карта порта 2', url: '/deployport/port2.png' },
    { id: 'mynewmap', name: 'Моя карта', url: '/deployport/mynewmap.png' }, // Добавьте эту строку
    { id: 'empty', name: 'Без карты', url: null }
];
```

### Способ 2: Загрузка через интерфейс

1. Нажмите кнопку "📁 Загрузить свою карту" в правом сайдбаре
2. Выберите файл изображения с вашего компьютера
3. Карта автоматически загрузится и отобразится на холсте

## Рекомендуемые характеристики изображений:

- **Формат**: PNG (для прозрачности) или JPG
- **Разрешение**: 1000x1000 пикселей или больше для лучшего качества
- **Размер файла**: до 5 МБ
- **Ориентация**: любая, но квадратные изображения работают лучше всего

## Текущая система координат:

- По умолчанию карта размещается в области от -200 до 200 по осям X и Y
- Размер можно изменить в файле `Map.jsx`, параметр `mapImage`

## Настройка позиции и размера карты:

В файле `src/components/Content/Map/Map.jsx` найдите:

```javascript
const [mapImage] = useState({
    width: 400,   // ширина в единицах координат
    height: 400,  // высота в единицах координат
    x: -200,      // позиция X (левый нижний угол)
    y: -200       // позиция Y (левый нижний угол)
});
```

Измените эти значения для настройки отображения вашей карты.
